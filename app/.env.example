# Copy this file to phototank/.env and edit values

# Root directory containing your organized photos (year/month/day/...) or any folder tree to scan
PHOTO_ROOT=/absolute/path/to/photo_root

# Folder you upload new photos into (PhotoTank will move them into PHOTO_ROOT on import)
# Optional: defaults to import
IMPORT_ROOT=import

# Folder where failed imports are moved for investigation
# Optional: defaults to failed
FAILED_ROOT=failed

# SQLite DB path (will be created if missing)
# Optional: defaults to data/phototank.sqlite
DB_PATH=data/phototank.sqlite

# Root directory for generated derivatives (NOT under PHOTO_ROOT)
# Optional: defaults to data/derivatives
DERIV_ROOT=data/derivatives

# Derivative sizing (max dimension, aspect preserved)
THUMB_MAX=256
MID_MAX=2048

# WebP qualities
THUMB_QUALITY=75
MID_QUALITY=85

# Optional: comma-separated extensions to scan
# PHOTO_EXTS=.jpg,.jpeg,.tif,.tiff,.png,.heic,.webp

# Optional: when EXIF date is missing, try other sources for datetime_original.
# Comma-separated values: json,mtime
# DATETIME_FALLBACK=json,mtime

# Optional: point PhotoTank at a different env file without moving/renaming files
# PHOTOTANK_ENV_FILE=app/.env.other

# Logging
# LOG_LEVEL=INFO
# LOG_FILE=data/phototank.log
# LOG_FILE_MAX_BYTES=10000000
# LOG_FILE_BACKUPS=5
# LOG_SYSLOG_HOST=logs.example.com
# LOG_SYSLOG_PORT=514
# LOG_SYSLOG_PROTOCOL=udp
# LOG_SYSLOG_PATH=/var/run/syslog
